---
"$id": "user.UserCreateRequest.API"
type: object
required:
- username
- name
properties:
  fields:
    username:
      "$ref": "user.User"
      property: "username"
    name:
      "$ref": "user.User"
      property: "name"
# TODO: Сделать как в task.User.Lookup
# TODO: Убрать API в названии - это модель
